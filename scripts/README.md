Active command-line entry points live here.

- `run_inference.py`: unified CLI for pipeline, classifier, YOLO, and Mask R-CNN inference

Example:

```bash
python scripts/run_inference.py pipeline --image test_data/images/samples/normal_002.JPG
```
