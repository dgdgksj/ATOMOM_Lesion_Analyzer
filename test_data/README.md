Active sample assets live here.

- `images/`: the main image dataset and curated `samples/` used by smoke tests
- `sample_images/`: a smaller legacy image subset kept for quick manual checks
- `videos/`: video samples for frame extraction and video-based experiments
- `archives/`: zipped image bundles and segmentation/raw archives
