"""Database models are intentionally omitted for the active inference app.

The previous product and ingredient schema was archived under `legacy/`
because the current runtime only serves lesion analysis.
"""
